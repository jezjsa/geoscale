# Supabase Configuration
# These are safe to expose in the client (public keys)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Note: All sensitive API keys (Google, DataForSEO, OpenAI, Stripe secret keys)
# will be stored in Supabase Secrets and accessed via Edge Functions
# DO NOT store sensitive keys in environment variables

# For local development, you may need:
# VITE_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key (safe to expose)
